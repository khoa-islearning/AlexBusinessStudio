<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">AlexBusinessStudio</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass" @onclick="ToggleNavMenu">
    <nav class="flex-column">
        <!--NEED TO ADD LINK AFTER-->
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <RadzenIcon Icon="pie_chart" IconStyle="IconStyle.Light"/> &nbsp;Sales Summary
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="/storage" Match="NavLinkMatch.All">
                 <RadzenIcon Icon="folder" IconStyle="IconStyle.Light"/>&nbsp;Storage
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="/listings" Match="NavLinkMatch.All">
                 <RadzenIcon Icon="view_list" IconStyle="IconStyle.Light"/>&nbsp;Listings
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="/history" Match="NavLinkMatch.All">
               <RadzenIcon Icon="history" IconStyle="IconStyle.Light"/> &nbsp;Sales History
            </NavLink>
        </div>

    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
